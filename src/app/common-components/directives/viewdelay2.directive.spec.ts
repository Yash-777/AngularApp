import { Viewdelay2Directive } from './viewdelay2.directive';

describe('Viewdelay2Directive', () => {
  it('should create an instance', () => {
    const directive = new Viewdelay2Directive();
    expect(directive).toBeTruthy();
  });
});
